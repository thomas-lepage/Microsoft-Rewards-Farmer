version: '3'
services:
  rewards:
    image: severinonly/ms-rewards:latest
    init: true
    privileged: true
    network_mode: "host"
    restart: always
    environment:
      SUPERVISOR_TOKEN: 'docker'
      TZ: 'America/Montreal'
    volumes:
      - ../src:/app/src